## Packages
framer-motion | Smooth layout animations and result reveals
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
The backend API endpoint is /api/analyze-focus (POST).
The Simulator page allows users to test the AI logic manually by pasting content.
